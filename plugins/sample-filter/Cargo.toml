[package]
name = "sample-filter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../crates/plugin-sdk" }
shared-types = { path = "../../crates/shared-types" }
serde_json = "1.0"
